[build]
  command = "npm run build"
  publish = "www"

[build.environment]
  NODE_VERSION = "20"

# Single-page app fallback: serve index.html for unknown paths
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
